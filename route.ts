export const publicRoutes = ["/", "/customer", "/reserve"];

export const authRouter = ["/auth/login", "/auth/register"];

export const apiAuthPrefix = "/api/auth";

export const DEFAULT_LOGIN_REDIRECT = "/dashboard/orders";
